# Product

## Register

product

## Users

推荐系统、推理服务和基础设施工程师。他们在部署、压测、故障定位和性能评审中，需要快速理解请求经过哪些服务、并发压力在哪里产生，以及业务时延如何归属。

## Product Purpose

PaiRec4TigerLLM 是一个集成生成式召回、向量召回、DeepFM Rank、DataSystem KV Cache 和可控 BRPC 压力注入的端到端推荐系统。成功标准是业务链路可运行、压力契约可验证、时延可归因，并且部署拓扑能够被工程人员快速审查。

## Brand Personality

严谨、克制、可审计。信息表达应优先服务技术判断，不使用营销式夸张或装饰性视觉。

## Anti-references

避免自由连线的网络拓扑图、交叉回流线、层层嵌套的卡片、仅靠颜色区分含义，以及把单次压测数据包装成稳定结论的仪表盘式版面。

## Design Principles

1. 业务主链必须能从左到右一次读完。
2. 业务、压力和 KVC 数据面使用独立泳道，不让辅助流量遮挡主链。
3. 物理归属、端口和并发契约就近标注，减少跨图查找。
4. 实测、目标和未验证状态必须明确区分。
5. 图表优先保证可读性、对比度和打印可用性。

## Accessibility & Inclusion

关键语义同时使用文字、线型和颜色编码；正文保持高对比度；不依赖动画；在 1920 像素宽度和缩放查看时均保持文字可辨识。
